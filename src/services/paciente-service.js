/*const ETRepository = require('../repositories/paciente-repositorires');

const obtenerPaciente = async (idTurno) => {
  return await ETRepository.obtenerPaciente(idTurno);
};

module.exports = { obtenerPaciente };*/